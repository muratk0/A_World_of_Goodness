import React from 'react';

// Using a div instead of fragment since the original App returned a div
function HomePage() {
    return (
        <main style={{ textAlign: 'center', marginTop: '50px' }}>
            {/* Main content will go here */}
        </main>
    );
}

export default HomePage;
